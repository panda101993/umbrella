const path = '../../assets/images/'
const ext = '.png'

export default Icons ={
    landing_image: require(`${path}landing_image${ext}`),
    Umbrellalogo: require(`${path}umbrellalogo${ext}`),
// headerimage: require(`${path}header_image${ext}`)
}