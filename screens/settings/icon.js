
const path = '../../assets/images/'
const ext = '.png'

export default Icons ={

    dropDownIcon: require(`${path}dropdown${ext}`),
    popupIcon: require(`${path}popup${ext}`),
    phoneNumber: require(`${path}download${ext}`),
    emailIcon: require(`${path}email${ext}`),
    profile: require(`${path}layer47${ext}`),
    Receipthistory: require(`${path}layer49${ext}`),
    Productenqury: require(`${path}layer51${ext}`),
    feedback: require(`${path}layer52${ext}`),
    logout: require(`${path}layer63${ext}`),
    Aboutus: require(`${path}layer59${ext}`),
    Faqs: require(`${path}layer61${ext}`),
    Privacypolicy: require(`${path}layer57${ext}`),
    Resetpassword:require(`${path}layer54${ext}`),
    OrderHistory:require(`${path}layer50${ext}`),
    Termandcondition:require(`${path}layer55${ext}`),
}