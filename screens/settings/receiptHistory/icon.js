const path = '../../../assets/images/'
const ext = '.png'

export default Icons ={
      
// logo: require(`${path}forgotpassword_icon${ext}`),
calander: require(`${path}layer34${ext}`),
Download:require(`${path}layer36${ext}`),
watch: require(`${path}layer35${ext}`), 
}