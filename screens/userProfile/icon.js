
const path = '../../assets/images/'
const ext = '.png'

export default Icons ={

    Profile: require(`${path}profile_icon${ext}`),
    cameraimage: require(`${path}camera_icon${ext}`),
    unchecked: require(`${path}checked${ext}`),
    checked: require(`${path}unchecked${ext}`),
}